# 🎮 Number Guessing Game (Python)

A simple command-line number guessing game built with Python.

The player must guess a randomly generated number within a limited number of attempts. The game provides feedback after each guess and calculates a score based on performance.

---

## 🚀 Features

- Random number generation
- Limited attempts system
- Input validation (prevents crash on invalid input)
- Score calculation system
- Clean function-based structure

---

## 🛠 Technologies Used

- Python 3
- Built-in `random` module

---

## ▶️ How to Run

1. Make sure Python 3 is installed.
2. Run the following command in your terminal:

```bash
python number_guessing_game.py
